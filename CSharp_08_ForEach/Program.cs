// we use foreach loop to iterate over a collection of items. The collection can be an array, list, dictionary, etc.

int[] numbers = new int[] { 5, 33, 1, 7, 31, 73, 31, 98 };

foreach(var number in numbers)
{
    Console.WriteLine("Ackuse Academy");
}